{
  "name": "@mypi/agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "dependencies": {
    "@mypi/ai": "0.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "smoke:openai": "node scripts/openai-agent-smoke.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build --workspace @mypi/ai && npm run build && vitest run --root ../.. package/agent/test"
  }
}
